language: java

jdk:
  - openjdk8

branches:
  only:
    - master

before_script:
  - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  - chmod +x ./cc-test-reporter
  - ./cc-test-reporter before-build

script: ./gradlew check

after_success:
  # upload report to codecov
  - bash <(curl -s https://codecov.io/bash)
  # publish ascii doctor docs
  - ./gradlew asciidoctor
  - git submodule add -b gh-pages https://${GH_OAUTH_TOKEN}@github.com/${GH_OWNER}/${GH_PROJECT_NAME} site > /dev/null 2>&1
  - cd site
  - git rm -r .
  - cp -R ../build/asciidoc/html5/* .
  - git add -f .
  - git config user.name "AlphaHinex"
  - git config user.email "AlphaHinex@gmail.com"
  - DATE=`date +%Y-%m-%d`
  - TIME=`date +%H:%M:%S`
  - git commit -am "Travis CI synchronized gh-pages at $DATE $TIME"
  - git push https://${GH_OAUTH_TOKEN}@github.com/${GH_OWNER}/${GH_PROJECT_NAME} HEAD:gh-pages > /dev/null 2>&1

#after_script:
#  - JACOCO_SOURCE_PATH=modules/blocks/roll-utils/src/main/java ./cc-test-reporter format-coverage -t jacoco -o coverage/codeclimate.utils.json modules/blocks/roll-utils/build/reports/jacoco/test/jacocoTestReport.xml
#  - JACOCO_SOURCE_PATH=modules/raw-materials/roll-web/src/main/java ./cc-test-reporter format-coverage -t jacoco -o coverage/codeclimate.web.json modules/raw-materials/roll-web/build/reports/jacoco/test/jacocoTestReport.xml
#  - ./cc-test-reporter sum-coverage coverage/codeclimate.*.json -p 2
#  - ./cc-test-reporter upload-coverage