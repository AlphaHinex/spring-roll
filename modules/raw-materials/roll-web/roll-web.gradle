apply plugin: 'java-library'

dependencies {
                   api libraries.spring_boot_starter_web

                   api project(':roll-base'),
                       project(':roll-utils')

    testImplementation project(':roll-test')
}